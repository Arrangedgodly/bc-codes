// Stub Worker entry for @cloudflare/vitest-plugin (see vitest.config.ts):
// keeps unit tests independent of the SvelteKit build output. The test code
// itself runs inside this worker; no request handler is needed.
export default {};
