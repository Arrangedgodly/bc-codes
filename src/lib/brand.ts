/**
 * Brand strings — single point of change.
 *
 * PRODUCT.md: "bc-codes" is a WORKING TITLE. Nothing may bake the name into
 * markup, comments, or assets; every surface reads it from here (or receives it
 * as a prop). The wordmark-agnostic layout renders whatever string this is.
 */
export const APP_NAME = 'bc-codes';

/** Rail tagline under the name — console register, not a marketing line. */
export const APP_TAGLINE = 'bandcamp code console';

/**
 * Decorative JP micro-label for the console rail. The bilingual layer is the
 * world's signature but is NEVER load-bearing: render with aria-hidden.
 */
export const APP_TAGLINE_JP = '配信コンソール';
